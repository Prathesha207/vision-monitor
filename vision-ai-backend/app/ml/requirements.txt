# --- inference (analyzer.py) ---
ultralytics>=8.3.0
opencv-python>=4.5.0
imageio-ffmpeg>=0.6.0
numpy>=1.21.0
scipy>=1.7.0
PyYAML>=5.4.0
mediapipe==0.10.14

# --- training (train.py) ---
# Use setup_linux.sh or setup_windows.ps1 for deployment. Those scripts install
# this file first, then replace generic PyTorch with CUDA PyTorch when NVIDIA
# is available.
torch>=2.0.0
torchvision>=0.15.0
psutil>=5.9.0

# --- dataset prep (prepare_dataset.py) ---
autodistill==0.1.29
autodistill-grounding-dino